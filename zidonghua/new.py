# 如果将类比喻为工厂，那么__init__()方法则是该工厂的生产工人，__init__()方法接受的初始化参 数则是生产所需原料，
# __init__()方法会按照方法中的语句负责将原料加工成实例以供工厂出货。而 __new__()则是生产部经理，
# __new__()方法可以决定是否将原料提供给该生产部工人，
# 同时它还决定着出 货产品是否为该生产部的产品，因为这名经理可以借该工厂的名义向客户出售完全不是该工厂的产品。

class A(object):
    def __init__(self):
        print('init方法被执行')
        print(a)

    # new方法是用来创建实例对象的，必须返回一个实例对象，调用了父类的方法，返回给它
    def __new__(cls,*args,**kwargs):
        print('new方法被执行')
        return object.__new__(cls)
a=1


b = A()
